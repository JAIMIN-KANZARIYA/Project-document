#Run Phpmyadmin in ubuntu
Lampp start: $ sudo /opt/lampp/manager-linux-x64.run

Php version check: $ php --version

Composer version check: $ composer

Laravel install: composer create-project --prefer-dist laravel/laravel project_name

Create Model and migration: php artisan make:model Company -m

Create Controller: php artisan make:controller CompanyController

Create New Field Using Migrations = > php artisan make:migration add_paid_to_users_table

Create New Table Using Migrations = > php artisan make:migration create_users_table

Storage Link => php artisan storage:link

Change Field Using Migrations = > $table->string('google_user_id')->change();

Relationship One to One = > return $this->hasOne(Model::class, 'foreign_key', 'local_key');

After add colunm or create table migrate command : php artisan migrate

Seeder Run = > php artisan db:seed --class=UserSeeder 

Seeder Create = > php artisan make:seeder UserSeeder

Laravel Hide .env file htaccess entry = >   <Files .env>
                                            Order allow,deny
                                            Deny from all
                                            </Files>

